import fs from 'node:fs';
import { buildSync } from 'esbuild';
import { renderResources } from './resource-templates.mjs';
const resources=JSON.parse(fs.readFileSync('content/project-resources.json','utf8'));
const resourceSources=Object.assign({},...Object.values(resources).map(r=>r.existingSources));
const projects=JSON.parse(fs.readFileSync('content/projects.json','utf8'));
const projectDates=Object.fromEntries(JSON.parse(fs.readFileSync('content/project-dates.json','utf8')).map(record=>[record.project,record]));
const site=JSON.parse(fs.readFileSync('content/site.json','utf8'));
const assets=JSON.parse(fs.readFileSync('content/assets.json','utf8'));
const sizes=fs.existsSync('content/image-sizes.json')?JSON.parse(fs.readFileSync('content/image-sizes.json','utf8')):{};
const responsive=JSON.parse(fs.readFileSync('content/responsive-images.json','utf8'));
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const dateFormat=new Intl.DateTimeFormat('en-NZ',{day:'numeric',month:'short',year:'numeric',timeZone:'UTC'});
const dateLabel=p=>projectDates[p.id].sourceType==='project'?'Project year':'Last updated';
const projectDate=p=>projectDates[p.id].sourceType==='project'?esc(projectDates[p.id].year):`<time datetime="${esc(projectDates[p.id].committedAt)}">${dateFormat.format(new Date(projectDates[p.id].committedAt))}</time>`;
const projectDateSource=p=>projectDates[p.id].sourceType==='repository'?`<span class="date-source-note">Latest commit in the project repository.</span>${external(projectDates[p.id].source,'View commit','commit-source-link')}`:'';
const arrow='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><path d="M5 19 19 5M5 5h14v14"/></svg>';
const right='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><path d="M4 12h15m-6-6 6 6-6 6"/></svg>';
const paths={chart:'M4 20h4v-7H4zm6 0h4V8h-4zm6 0h4V3h-4z',cube:'m12 2 9 5v10l-9 5-9-5V7zm0 10L3 7m9 5 9-5m-9 5v10',system:'M8 3H3v5m13-5h5v5M3 16v5h5m13-5v5h-5M8 8h8v8H8zM1 12h5m12 0h5M12 1v5m0 12v5',leaf:'M20 3C8 2 2 8 5 15c2 6 14 8 15-12ZM4 21 16 8',spark:'m12 2 3 7 7 3-7 3-3 7-3-7-7-3 7-3z',rings:'M14 12a6 6 0 1 1-12 0 6 6 0 0 1 12 0Zm8 0a6 6 0 1 1-12 0 6 6 0 0 1 12 0Z',people:'M9 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8Zm8 0a3 3 0 1 0 0-6M2 21v-3a7 7 0 0 1 14 0v3m2-7a5 5 0 0 1 4 4v3'};
const icon=name=>`<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linejoin="round" stroke-linecap="round" aria-hidden="true"><path d="${paths[name]}"/></svg>`;
const external=(href,label,cls='text-link')=>`<a class="${cls}" href="${esc(href)}" target="_blank" rel="noopener noreferrer">${esc(label)} ${arrow}<span class="sr-only"> (opens in a new tab)</span></a>`;
function img(src,alt,base='',opts={}) {
 const [w,h]=sizes[src]||[1200,800];
 const variants=responsive[src];
 const imageSizes=opts.class==='hero-image'?'(max-width: 760px) 90vw, (max-width: 1200px) 40vw, 480px':opts.eager?'90vw':'(max-width: 480px) 90vw, (max-width: 1100px) 45vw, 40vw';
 const srcset=variants?` srcset="${variants.map(v=>`${base+encodeURI(v.src)} ${v.width}w`).join(', ')}" sizes="${imageSizes}"`:'';
 return `<img src="${base+encodeURI(src)}"${srcset} alt="${esc(alt)}" width="${w}" height="${h}" ${opts.eager?'loading="eager" fetchpriority="high"':'loading="lazy"'} decoding="async"${opts.class?` class="${opts.class}"`:''}>`;
}
const button=(url,label,secondary=false)=>`<a class="button${secondary?' button-outline':''}" href="${esc(url)}"><span>${label}</span>${arrow}</a>`;
const eyebrow=(num,text)=>`<div class="eyebrow"><span class="section-no">${num}</span><span>${text}</span></div>`;
function header(active,base) {return `<a class="skip-link" href="#main">Skip to content</a><header class="site-header"><div class="nav-wrap"><a class="brand" href="${base}index.html" aria-label="YWS. Young Woo Song — home">YWS<span class="brand-dot">.</span></a><span class="brand-caption">AI × AEC × SYSTEMS</span><nav class="site-nav" id="site-nav" aria-label="Main navigation">${[['index','Home'],['projects','Projects'],['about','About'],['contact','Contact']].map(([id,name])=>`<a href="${base}${id}.html" ${active===id?'aria-current="page"':''}>${name}</a>`).join('')}</nav><a class="nav-contact" href="mailto:${site.email}">Let’s talk ${arrow}</a><button class="menu-toggle" aria-controls="site-nav" aria-expanded="false" aria-label="Open navigation"><span></span><span></span></button></div></header>`;}
function footer(base) {return `<footer class="site-footer"><div class="wrap footer-top"><a class="brand" href="${base}index.html" aria-label="YWS. Young Woo Song — home">YWS<span class="brand-dot">.</span></a><p>Applied AI for AEC.<br>Practical tools. Smarter workflows.</p><div class="footer-links">${external(site.github,'GitHub')}${external(site.linkedin,'LinkedIn')}<a class="text-link" href="mailto:${site.email}">Email ${arrow}</a></div></div><div class="wrap footer-bottom"><span>© ${new Date().getFullYear()} Young Woo Song</span><span>Auckland, New Zealand · Aotearoa</span><button class="motion-toggle" hidden aria-pressed="false">Motion: on</button><a href="#top">Back to top ↑</a></div></footer>`;}
function connect(base) {return `<section class="connect-band" id="contact"><div class="wrap connect-grid">${eyebrow('LET’S CONNECT','SAME CURIOSITY. A BRIGHTER TOMORROW.')}<h2 data-reveal>Good things begin<br>with a <em>conversation.</em></h2><div class="connect-copy"><p>Have an idea, a challenge, or a different perspective?<br>I’d love to hear it.</p>${button(base+'contact.html','Let’s make a connection')}</div></div></section>`;}
function shell({title,description,active='projects',base='',url='index.html',body,cover='images/brand/aec-connected-intelligence.webp',noindex=false,extra=''}) {
 return `<!doctype html><html lang="en-NZ"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="theme-color" content="#f5f3ed"><title>${esc(title)} | Young Woo Song</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${site.url+url}">${noindex?'<meta name="robots" content="noindex">':''}<meta property="og:type" content="website"><meta property="og:title" content="${esc(title)} | Young Woo Song"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${site.url+url}"><meta property="og:image" content="${site.url+encodeURI(cover)}"><meta name="twitter:card" content="summary_large_image"><link rel="icon" type="image/svg+xml" href="${base}favicon.svg"><link rel="preload" href="${base}fonts/manrope-latin.woff2" as="font" type="font/woff2" crossorigin>${url==='index.html'?`<link rel="preload" as="image" href="${base}images/brand/aec-connected-intelligence.webp" imagesrcset="${responsive['images/brand/aec-connected-intelligence.webp'].map(v=>`${base+v.src} ${v.width}w`).join(', ')}" imagesizes="(max-width: 760px) 90vw, (max-width: 1200px) 40vw, 480px" fetchpriority="high">`:''}<link rel="stylesheet" href="${base}styles/site.css"><script src="${base}js/page-transitions.js"></script><script type="module" src="${base}js/site.js"></script>${extra}</head><body id="top" data-page="${active}"><div class="device-frame" aria-hidden="true"><span class="device-camera"></span><span class="device-home-indicator"></span></div><div class="reading-progress" aria-hidden="true"></div>${header(active,base)}<main id="main">${body}</main>${footer(base)}<dialog class="lightbox" aria-label="Image viewer"><div class="lightbox-toolbar"><span class="lightbox-counter"></span><a class="lightbox-original" href="${base}projects.html" target="_blank" rel="noopener noreferrer">Open original ↗</a><button class="lightbox-close" aria-label="Close image viewer">Close ×</button></div><div class="lightbox-zoom" role="group" aria-label="Image zoom"><button class="zoom-out" aria-label="Zoom out">−</button><button class="zoom-fit" aria-label="Fit image to viewer">Fit</button><button class="zoom-in" aria-label="Zoom in">+</button><span class="zoom-level" aria-live="polite">100%</span><span class="zoom-help">Zoom in, then scroll to explore</span></div><div class="lightbox-canvas"><img class="lightbox-image" alt=""></div><div class="lightbox-bottom"><button class="lightbox-prev" aria-label="Previous image">←</button><p class="lightbox-caption" aria-live="polite"></p><button class="lightbox-next" aria-label="Next image">→</button></div></dialog></body></html>`;
}
function card(p,base='') {return `<article class="project-card" data-category="${esc(p.discipline)}" data-search="${esc([p.title,p.subtitle,p.blurb,...p.tech].join(' ').toLowerCase())}" data-reveal><a href="${base}projects/${p.slug}.html" class="project-image" aria-label="Explore project: ${esc(p.title)}">${img(p.cover,p.title,base)}<span class="image-cta">Explore project ${arrow}</span></a><div class="project-meta"><span>${esc(p.discipline)}</span><span class="project-date">${dateLabel(p)} ${projectDate(p)}</span></div><h3><a href="${base}projects/${p.slug}.html">${esc(p.title)} ${arrow}</a></h3><p>${esc(p.blurb)}</p><div class="card-bottom"><span>${esc(p.tech.slice(0,3).join(' · '))}</span>${p.repository?external(p.repository,'GitHub','repository-link'):`<a class="repository-link" href="${base}projects/${p.slug}.html">View study ${right}</a>`}</div></article>`;}
const services=[['chart','AI & data solutions','Practical intelligence. Meaningful decisions.','Turning information into useful tools, insights, and clearer decisions for AEC teams.'],['cube','Generative AI','AI for visual workflows.','Developing and evaluating image-generation tools for AEC workflows, from input preparation to useful outputs.'],['system','Systems & automation','Less friction. More possibility.','Connecting tools, knowledge, and teams through intelligent agents and thoughtful automation.'],['leaf','Governance & implementation','Built to work in the real world.','Supporting responsible adoption with clear standards, evaluation, and people at the centre.']];
function serviceGrid() {return `<div class="services-grid">${services.map(([ic,title,sub,desc],i)=>`<article class="service" data-reveal><div class="service-top"><span class="icon-disc">${icon(ic)}</span><span class="micro">0${i+1}</span></div><h3>${title}</h3><p class="service-subtitle">${sub}</p><p>${desc}</p></article>`).join('')}</div>`;}
function home() {return `<div class="tablet-journey" data-tablet-journey><section class="tablet-hero wrap" aria-labelledby="hero-title" data-tablet-hero>
  <div class="tablet-hero-heading">
    <span class="micro">YOUNG WOO SONG <span aria-hidden="true">/</span> AI SPECIALIST</span>
    <span class="micro">AUCKLAND, NEW ZEALAND</span>
  </div>
  <div class="tablet-stage">
    <div class="tablet-reveal">
      <div class="tablet-zoom"><div class="hero-tablet">
        <span class="tablet-camera" aria-hidden="true"></span>
        <div class="tablet-screen">
          <div class="tablet-topbar"><span class="tablet-monogram">YWS<span>.</span></span><span>AI <span aria-hidden="true">×</span> DATA <span aria-hidden="true">×</span> SYSTEMS</span></div>
          <div class="tablet-layout">
            <div class="tablet-copy">
              <span class="eyebrow tablet-intro" data-tablet-content>AI SPECIALIST FOR THE AEC INDUSTRY</span>
              <h1 id="hero-title" data-tablet-content>Practical<br>intelligence.<br><em>For AEC.</em></h1>
              <p class="tablet-description" data-tablet-content>I develop AI tools, intelligent systems, and automation for architecture, engineering, and construction.</p>
              <div class="tablet-actions" data-tablet-content>${button('projects.html','Explore my work')}${external(site.github,'GitHub')}</div>
            </div>
            <div class="tablet-art tablet-art--ai">
              ${img('images/brand/aec-connected-intelligence.webp','Connected AEC documents, data layers, image-generation controls and visual outputs — AI-generated concept illustration','',{eager:true,class:'hero-image'})}
              <div class="tablet-art-caption"><span class="micro">INDUSTRY CONTEXT.<br>PRACTICAL AI EXPERTISE.</span><span aria-hidden="true">↗</span></div>
              <span class="tablet-image-credit">AI-generated concept illustration</span>
            </div>
          </div>
        </div>
      </div>
      </div><span class="tablet-ground-shadow" aria-hidden="true"></span>
    </div>
    <div class="tablet-baseline" aria-hidden="true"><span class="tablet-line"></span><span class="tablet-slot"></span><span class="tablet-line-tick"></span><span class="tablet-line-tick"></span></div>
  </div>
  <div class="tablet-hero-footer"><span class="micro">AEC EXPERTISE. INTELLIGENT SYSTEMS.</span><a href="#perspective">Scroll to discover <span aria-hidden="true">↓</span></a></div>
</section></div><div class="home-content">
<section class="perspective section" id="perspective"><div class="wrap perspective-grid">${eyebrow('01','THE CONNECTING THREAD')}<div><h2 data-reveal>Technology moves forward.<br><span class="muted">Let’s make sure it brings</span><br>people <em>with it.</em></h2><div class="perspective-bottom"><p>I specialise in applying AI to the architecture, engineering, and construction industry. I build tools around the knowledge, workflows, and practical needs of AEC teams.</p><a class="text-link" href="about.html">A little about me ${arrow}</a></div></div></div></section>
<section class="section selected-work" id="work"><div class="wrap"><div class="section-heading">${eyebrow('02','SELECTED WORK')}<div class="heading-row"><h2 data-reveal>Ideas, made <em>tangible.</em></h2><a class="text-link" href="projects.html">All 13 projects ${arrow}</a></div><p>A selection of past personal projects exploring AI tools, automation systems, and research, with a focus on the AEC industry.</p></div><div class="featured-grid">${['ai-visualizations','codevision','n8n-rag'].map(id=>card(projects.find(p=>p.id===id))).join('')}</div></div></section>
<section class="section capabilities" id="expertise"><div class="wrap"><div class="section-heading">${eyebrow('03','WHAT I DO')}<div class="heading-row"><h2 data-reveal>From insight<br>to <em>real impact.</em></h2><p class="section-aside">A practical approach to AI,<br>automation, and implementation.</p></div></div>${serviceGrid()}</div></section>
<section class="manifesto manifesto--ai"><div class="manifesto-image">${img('images/brand/aec-generative-workflows.webp','Sketches and material references flowing through an AI tool into generated image variations — conceptual illustration')}<span class="image-credit">AI-generated concept illustration</span></div><div class="manifesto-copy">${eyebrow('04','APPLIED AI IN AEC')}<h2 data-reveal>Turning<br>AI potential<br><em>into practice.</em></h2><p>AI tools that help AEC teams retrieve knowledge, generate and refine imagery, and automate everyday workflows.</p><a class="text-link" href="projects.html">Explore my AI projects ${arrow}</a></div></section>${connect('')}</div>`;}
const disciplines=[...new Set(projects.map(p=>p.discipline))];
function listing() {return `<section class="page-intro wrap">${eyebrow('PERSONAL PROJECT ARCHIVE','EXPERIMENTS. SYSTEMS. POSSIBILITIES.')}<div class="intro-row"><h1 data-title>Curiosity,<br><em>put to work.</em></h1><p>From intelligent agents to generative AI workflows. A collection of ${projects.length} past personal projects exploring AI, data, and practical industry challenges.</p></div></section><section class="work-index wrap" aria-label="Project collection"><div class="project-tools" data-filters hidden><div class="filter-heading"><span class="micro">EXPLORE BY DISCIPLINE</span><label class="search-box"><span class="sr-only">Search projects</span><svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="10" cy="10" r="6"/><path d="m15 15 5 5"/></svg><input type="search" id="project-search" placeholder="Search projects or tools…" autocomplete="off"></label></div><div class="filter-list" role="group" aria-label="Filter projects by discipline">${['All projects',...disciplines].map((c,i)=>`<button class="filter-button${i===0?' active':''}" data-filter="${esc(i===0?'all':c)}" aria-pressed="${i===0}">${c}<span>${i===0?projects.length:projects.filter(p=>p.discipline===c).length}</span></button>`).join('')}</div></div><div class="results-row"><p id="project-count" role="status" aria-live="polite">Showing all ${projects.length} projects</p><span class="micro">BUILT WITH CURIOSITY</span></div><div class="project-grid">${[projects.at(-1),...projects.slice(0,-1)].map(p=>card(p)).join('')}</div><div class="empty-state" hidden><h2>No projects found.</h2><p>Try another keyword or explore a different discipline.</p><button class="button reset-filters">Clear filters ${right}</button></div></section>${connect('')}`;}
const galleryGroups={text_to_render:'Text to render',model_to_render:'Model to render',sketch_to_render:'Sketch to render',plan_to_render:'Plan to render',masterplan_render:'Masterplan studies',ComfyUI_workflow:'ComfyUI experiments',bg_add_render:'Context & interior studies','Project imagery':'Project imagery'};
function gallery(items,base,group='gallery') {return `<div class="gallery-grid">${items.map((a,i)=>`<figure data-reveal><a class="gallery-open" href="${base+encodeURI(a.source||a.src)}" data-gallery="${esc(group)}" data-full="${base+encodeURI(a.src)}" data-caption="${esc(a.caption||a.alt)}" aria-label="Enlarge ${esc(a.alt)}">${img(a.src,a.alt,base)}<span class="gallery-expand" aria-hidden="true">↗</span></a><figcaption><span>${String(i+1).padStart(2,'0')}</span>${esc(a.caption||a.alt)}${resourceSources[a.source||a.src]?external(resourceSources[a.source||a.src],'GitHub source','gallery-source-link'):''}</figcaption></figure>`).join('')}</div>`;}
function about() {return `<section class="page-intro wrap">${eyebrow('ABOUT YOUNG WOO SONG','AI SPECIALISM. AEC INDUSTRY KNOWLEDGE.')}<div class="intro-row"><h1 data-title>AI specialist.<br><em>AEC focus.</em></h1><p>Applying artificial intelligence to the workflows and challenges of architecture, engineering, and construction.</p></div></section><section class="wrap about-story"><div class="about-art">${img('images/brand/coastal-architecture.webp','Concrete architecture and coastal landscape — AI-generated brand artwork','',{eager:true})}<span class="about-monogram">YWS</span><span class="image-credit">AI-generated brand study</span></div><div class="about-copy">${eyebrow('01','A CONNECTED PERSPECTIVE')}<h2 data-reveal>Same curiosity.<br><em>New possibilities.</em></h2><p class="lead">I’m Young Woo Song, an AI specialist focused on the AEC industry, based in Auckland.</p><p>My background in architectural practice gives me first-hand understanding of AEC workflows and constraints. Today, I use that industry knowledge to develop practical AI tools and systems.</p><p>My work spans practical internal tools, knowledge retrieval, workflow automation, and generative AI. Alongside the technology, I’m interested in the standards, governance, and everyday habits that help teams use it well.</p><div class="inline-links">${external(site.linkedin,'Connect on LinkedIn')}${external(site.cv,'View CV')}</div></div></section><section class="section wrap"><div class="section-heading">${eyebrow('02','WHAT GUIDES ME')}<h2 data-reveal>Grounded in <em>people.</em></h2></div><div class="values-grid">${[['spark','Curiosity','Always learning, always exploring what’s next.'],['rings','Integration','Bridging AI, industry knowledge, and everyday workflows.'],['chart','Impact','Turning ideas into meaningful, real-world outcomes.'],['people','Collaboration','Stronger solutions through diverse perspectives.']].map(([ic,t,d])=>`<article data-reveal><span class="icon-disc">${icon(ic)}</span><h3>${t}</h3><p>${d}</p></article>`).join('')}</div></section><section class="section experience-section"><div class="wrap two-column">${eyebrow('03','THE JOURNEY')}<div><h2 data-reveal>Built on experience.<br><em>Driven by learning.</em></h2><div class="timeline">${[['AI specialist','Ignite','Building practical AI tools and supporting responsible adoption across the AEC industry.'],['Architecture background','RCG · Woods Bagot · Ignite','Previous architectural experience across different project scales, stages, and collaborative teams.'],['Data science & AI','Institute of Data · Continued professional learning','Developing the analytical and engineering skills behind intelligent tools.']].map(([t,s,d])=>`<article data-reveal><span class="timeline-dot"></span><div><span class="micro">${s}</span><h3>${t}</h3><p>${d}</p></div></article>`).join('')}</div>${external(site.linkedin,'Full experience & qualifications')}</div></div></section><section class="section wrap" id="tools"><div class="section-heading">${eyebrow('04','TOOLS & EXPERTISE')}<h2 data-reveal>The tools behind<br>the <em>thinking.</em></h2></div><div class="tool-grid">${[['Python & data','Python, SQL, pandas, scikit-learn, PyTorch'],['Agents & retrieval','LangChain, LangGraph, FastMCP, n8n, vector search'],['Generative AI','Stable Diffusion, ControlNet, ComfyUI, Gemini'],['Applications & cloud','TypeScript, React, Next.js, FastAPI, Azure, Google Cloud'],['AEC systems & integration','BIM workflows, document processing, automation, APIs'],['Adoption & governance','Evaluation, privacy controls, research, implementation']].map(([t,d],i)=>`<article data-reveal><span class="micro">0${i+1}</span><h3>${t}</h3><p>${d}</p></article>`).join('')}</div></section>${connect('')}`;}
function contact() {return `<section class="contact-page wrap"><div class="contact-intro">${eyebrow('LET’S CONNECT','PEOPLE. IDEAS. OPPORTUNITIES.')}<h1 data-title>A conversation<br>could be the<br><em>start of something.</em></h1><p class="lead">I’m open to collaborations and opportunities in applied AI, intelligent systems, and automation for the AEC industry.</p><span class="location-line"><span class="status-dot"></span> Auckland, New Zealand · Aotearoa</span></div><div class="contact-card"><span class="micro">SAY HELLO</span><a class="contact-email" href="mailto:${site.email}">${site.email}${arrow}</a><p>For a project, an opportunity, or a good conversation.</p><div class="contact-channels">${external(site.linkedin,'LinkedIn')}${external(site.github,'GitHub')}${external(site.cv,'CV / Résumé')}</div><div class="contact-note"><span class="short-rule"></span><p>Same curiosity.<br>A brighter tomorrow.</p><span class="large-mark">YWS.</span></div></div></section><div class="contact-landscape">${img('images/brand/coastal-architecture.webp','Warm coastal architecture — AI-generated brand artwork')}<span>APPLIED AI FOR AEC. PRACTICAL TOOLS. SMARTER WORKFLOWS.</span></div>`;}
function detail(p) {
 const base='../'; const d=p.details;
 const grouped=Object.groupBy(p.gallery,a=>a.group);
 const isVisual=p.id==='ai-visualizations';
 const hasResources=resources[p.id].items.length||p.gallery.length||p.videos.length||p.demos.length;
 const next=projects[(projects.indexOf(p)+1)%projects.length];
 return `<section class="case-intro wrap"><a class="back-link" href="../projects.html">← All projects</a><div class="case-meta"><span class="eyebrow">${esc(p.discipline)}</span><span class="micro">${esc(p.status)}</span><span class="case-date">${dateLabel(p)} ${projectDate(p)}</span></div><h1 data-title>${esc(p.title)}</h1><p class="case-subtitle">${esc(p.subtitle)}</p><div class="case-actions">${p.repository?external(p.repository,'View on GitHub','button'):button(p.gallery.length?'#project-gallery':'#overview',isVisual?'Explore the visual studies':'Explore the project')}${hasResources?'<a class="text-link" href="#resources">Explore project resources ↓</a>':''}${p.videos.length||p.demos.length?'<a class="text-link" href="#demos">Watch the demo ↓</a>':''}<span class="case-index">${String(projects.indexOf(p)+1).padStart(2,'0')} / ${projects.length}</span></div></section><div class="case-cover wrap${isVisual?' visual-cover':''}">${img(p.cover,p.title,base,{eager:true})}<span class="cover-caption">${esc(p.discipline)} / ${esc(p.title)}</span></div><section class="section wrap case-overview" id="overview"><aside class="case-sidebar"><span class="micro">PROJECT AT A GLANCE</span><dl><dt>${dateLabel(p)}</dt><dd class="project-date">${projectDate(p)}${projectDateSource(p)}</dd><dt>Project type</dt><dd>${esc(p.status)}</dd><dt>Discipline</dt><dd>${esc(p.discipline)}</dd><dt>Focus</dt><dd>${esc(p.subtitle)}</dd><dt>Technology</dt><dd class="tags">${p.tech.map(t=>`<span>${esc(t)}</span>`).join('')}</dd></dl><div class="source-note">${p.repository?external(p.repository,'Explore the source'):`<span class="micro">SOURCE AVAILABILITY</span><p>${esc(p.repositoryNote)}</p>`}${p.id==='codevision'?'<a class="text-link" href="vision_model.html">Open-source prototype '+arrow+'</a>':''}${p.id==='n8n-rag'?'<a class="text-link" href="#resource-ingestion-workflow">View ingestion workflow ↓</a><a class="text-link" href="#resource-agent-workflow">View agent workflow ↓</a>':''}</div></aside><div class="case-reading">${eyebrow('01','THE OVERVIEW')}<h2 data-reveal>${isVisual?'Exploring AI<br>for visual workflows.':'An idea with<br>a practical purpose.'}</h2><p class="lead">${esc(d.overview)}</p>${d.problem?`<div class="problem-block"><h3>The challenge</h3><p>${esc(d.problem)}</p></div>`:''}${d.approach?`<div class="approach-list">${d.approach.map((a,i)=>`<article data-reveal><span class="micro">0${i+1}</span><div><h3>${esc(a.t)}</h3><p>${esc(a.d)}</p></div></article>`).join('')}</div>`:''}</div></section>
${d.pipeline?`<section class="section pipeline-section"><div class="wrap">${eyebrow('02','HOW IT WORKS')}<h2 data-reveal>From input<br>to <em>insight.</em></h2><ol class="pipeline">${d.pipeline.map((s,i)=>`<li data-reveal><span class="pipeline-number">${String(i+1).padStart(2,'0')}</span><h3>${esc(s.t)}</h3><p>${esc(s.d)}</p></li>`).join('')}</ol></div></section>`:''}
${d.features||d.architecture?`<section class="section wrap technical-section"><div class="section-heading">${eyebrow('IN PRACTICE','CAPABILITIES & ARCHITECTURE')}<h2 data-reveal>The thinking<br>behind the <em>system.</em></h2></div><div class="features-grid">${(d.features||[]).map(f=>`<article data-reveal><h3>${esc(f.t)}</h3><p>${esc(f.d)}</p></article>`).join('')}</div>${d.architecture?`<details class="technical-details"><summary>Architecture & technical details <span>+</span></summary><ul>${d.architecture.map(a=>`<li>${esc(a)}</li>`).join('')}</ul>${d.facts?`<dl class="facts-grid">${d.facts.map(f=>`<div><dt>${esc(f.k)}</dt><dd>${esc(f.v)}</dd></div>`).join('')}</dl>`:''}</details>`:''}</section>`:''}
${d.comparison?`<section class="section wrap comparison-section"><h2>${esc(d.comparison.title)}</h2><div class="table-scroll" role="region" aria-label="Model comparison" tabindex="0"><table><caption>${esc(d.comparison.title)} — project results</caption><thead><tr>${d.comparison.columns.map(c=>`<th scope="col">${esc(c)}</th>`).join('')}</tr></thead><tbody>${d.comparison.rows.map(r=>`<tr>${r.map(c=>`<td>${esc(c)}</td>`).join('')}</tr>`).join('')}</tbody></table></div></section>`:''}
${renderResources(p,resources[p.id],{esc,external,img,eyebrow})}
${p.gallery.length?`<section class="section gallery-section" id="project-gallery"><div class="wrap"><div class="section-heading">${eyebrow('A CLOSER LOOK',isVisual?'THE VISUAL COLLECTION':'PROJECT GALLERY')}<div class="heading-row"><h2 data-reveal>${isVisual?'Possibilities, <em>pictured.</em>':'See it <em>in context.</em>'}</h2><p class="section-aside">${p.gallery.length} images${p.videos.length?` · ${p.videos.length} videos`:''}<br>Select an image to explore.</p></div>${isVisual?'<p>AI-generated explorations, with source sketches, plans, and models shown alongside the outputs.</p>':''}</div>${isVisual?`<nav class="gallery-index" aria-label="Jump to a visual collection">${Object.keys(grouped).map(g=>`<a href="#gallery-${g.replaceAll(' ','-')}">${esc(galleryGroups[g]||g)}</a>`).join('')}</nav>`:''}${Object.entries(grouped).map(([group,items])=>`<div class="gallery-group" id="gallery-${group.replaceAll(' ','-')}"><h3>${esc(galleryGroups[group]||group)} <span>${String(items.length).padStart(2,'0')}</span></h3>${gallery(items,base,p.id)}</div>`).join('')}</div></section>`:''}
${p.videos.length||p.demos.length?`<section class="section wrap" id="demos"><div class="section-heading">${eyebrow('IN MOTION','WALKTHROUGHS & DEMONSTRATIONS')}<h2 data-reveal>See the idea<br><em>come to life.</em></h2></div><div class="video-grid">${p.videos.map(v=>{const clean=v.src.split('#')[0];const poster=`images/video-posters/${clean.split('/').pop().replace('.mp4','.webp')}`;return `<figure><video controls playsinline preload="none" poster="${base+poster}" aria-label="${esc(v.caption)}"><source src="${base+encodeURI(clean)}" type="video/mp4"><a href="${base+encodeURI(clean)}">Open video</a></video><figcaption>${esc(v.caption)}</figcaption><p class="video-description">${isVisual?'AI-generated architectural sequence exploring space, materials, and atmosphere.':`Screen recording of the ${esc(p.title)} workflow in use.`}</p></figure>`;}).join('')}${p.demos.map((m,i)=>`<figure class="gif-demo"><button class="gif-play" hidden data-gif="${base+encodeURI(m.src)}" aria-label="Play demonstration: ${esc(m.caption||m.alt)}"><img src="${base}images/video-posters/${p.id}-${i}.webp" alt="${esc(m.alt)}" loading="lazy"><span>Play demonstration ${right}</span></button><noscript><a class="gallery-open" href="${base+encodeURI(m.src)}">${img(`images/video-posters/${p.id}-${i}.webp`,m.alt,base)}<span class="gallery-expand" aria-hidden="true">?</span></a><p>Open the animated demonstration</p></noscript><figcaption>${esc(m.caption||m.alt)}</figcaption></figure>`).join('')}</div></section>`:''}
${d.outcomes?`<section class="section outcomes-section"><div class="wrap two-column">${eyebrow('THE TAKEAWAYS','WHAT THE PROJECT EXPLORES')}<div><h2 data-reveal>From exploration<br>to <em>understanding.</em></h2><ul class="outcomes">${d.outcomes.map(o=>`<li>${esc(o)}</li>`).join('')}</ul>${d.metrics?`<div class="metrics">${d.metrics.map(m=>`<div><strong>${esc(m.v)}</strong><span>${esc(m.k)}</span></div>`).join('')}</div>`:''}</div></div></section>`:''}
<nav class="project-pagination" aria-label="Other projects"><div class="wrap"><div class="project-pagination-heading"><span class="micro">OTHER PROJECTS</span><a class="text-link" href="../projects.html">Back to all projects ${right}</a></div><a class="next-project-card" href="${next.slug}.html"><div class="next-project-thumbnail">${img(next.cover,'',base)}</div><div class="next-project-copy"><span class="micro">NEXT PROJECT</span><h2>${esc(next.title)}</h2><p>${esc(next.discipline)} <span aria-hidden="true">·</span> <span class="project-date">${dateLabel(next)} ${projectDate(next)}</span></p></div><span class="next-project-action">View case study ${right}</span></a></div></nav>${connect(base)}`;
}
fs.mkdirSync('js/vendor',{recursive:true});
buildSync({entryPoints:['scripts/motion-entry.mjs'],bundle:true,minify:true,format:'esm',target:'es2022',outfile:'js/vendor/anime.esm.min.js'});
fs.copyFileSync('node_modules/animejs/LICENSE.md','js/vendor/ANIME-LICENSE.md');
const pages=[
 ['index.html',{title:'AI Specialist for AEC',description:'Young Woo Song is an AI specialist focused on the architecture, engineering, and construction (AEC) industry. Explore AI tools, intelligent systems, and automation.',active:'index',body:home()}],
 ['projects.html',{title:'Projects',description:'Explore 13 past personal projects across AI applications, agents and automation, generative AI, computer vision, and data science.',body:listing()}],
 ['about.html',{title:'About',description:'Meet Young Woo Song, an Auckland-based AI specialist specialising in the architecture, engineering, and construction (AEC) industry.',active:'about',body:about()}],
 ['contact.html',{title:'Let’s connect',description:'Connect with Young Woo Song about applied AI, intelligent systems, and automation for the AEC industry.',active:'contact',body:contact()}],
 ...projects.map(p=>[`projects/${p.slug}.html`,{title:p.title,description:p.blurb,base:'../',body:detail(p),cover:p.cover}])
];
for(const [url,options] of pages)fs.writeFileSync(url,shell({...options,url}));
const errorBody=`<section class="wrap error-page">${eyebrow('404','A SMALL DETOUR')}<h1>We couldn’t find<br><em>that page.</em></h1><p>The page you’re looking for may have moved. Let’s find your way back.</p><div class="hero-actions">${button(site.url+'projects.html','Explore the projects')}${button(site.url,'Back to home',true)}</div></section>`;
fs.writeFileSync('404.html',shell({title:'Page not found',description:'Find your way back to the Young Woo Song portfolio.',body:errorBody,noindex:true,url:'404.html',base:new URL(site.url).pathname}));
const redirectMap=Object.fromEntries(projects.map(p=>[p.id,`projects/${p.slug}.html`]));
fs.writeFileSync('js/project-redirect.js',`const routes=${JSON.stringify(redirectMap)};\nconst id=new URLSearchParams(location.search).get('id');\nif(routes[id])location.replace(routes[id]+location.hash);\n`);
fs.writeFileSync('project.html',shell({title:'Find a project',description:'Explore the Young Woo Song project collection.',body:`<section class="wrap error-page">${eyebrow('PROJECT DIRECTORY','FIND YOUR NEXT EXPLORATION')}<h1>Ideas worth<br><em>exploring.</em></h1><p>This project link has moved. Choose a project below.</p><div class="redirect-projects">${projects.map(p=>`<a class="text-link" href="projects/${p.slug}.html">${esc(p.title)} ${arrow}</a>`).join('')}</div></section>`,url:'project.html',noindex:true,extra:'<script defer src="js/project-redirect.js"></script>'}));
fs.writeFileSync('sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${pages.map(([url])=>`<url><loc>${site.url+url}</loc></url>`).join('')}</urlset>`);
fs.writeFileSync('robots.txt',`User-agent: *\nAllow: /\nSitemap: ${site.url}sitemap.xml\n`);
fs.writeFileSync('.nojekyll','');
fs.writeFileSync('test-performance.html',shell({title:'Site diagnostics',description:'Local performance diagnostics for the portfolio.',active:'index',url:'test-performance.html',noindex:true,extra:'<script defer src="js/diagnostics.js"></script>',body:`<section class="page-intro wrap">${eyebrow('SITE DIAGNOSTICS','MAINTENANCE UTILITY')}<h1>Performance,<br><em>in perspective.</em></h1><p class="lead">Measurements for this page in your current browser. Results vary with device, connection, and cache.</p></section><section class="wrap section" style="padding-top:0"><h2>Browser measurements</h2><dl class="facts-grid" id="diagnostics" aria-live="polite"><div><dt>Status</dt><dd>Enable JavaScript to measure this page.</dd></div></dl><p>Use the project’s browser verification suite to check page layouts, navigation, accessibility, and asset loading across the portfolio.</p>${button('index.html','Return to the portfolio')}</section>`}));
console.log(`Built ${pages.length} pages, 404, legacy project routing, and sitemap.`);
