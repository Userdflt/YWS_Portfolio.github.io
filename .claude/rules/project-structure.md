# Project Structure

- Keep new code in existing top-level dirs; avoid new top-level dirs unless documented.

<!-- CUSTOMIZE: Replace the sections below with your actual project layout. Delete examples that don't apply. -->

## Frontend

<!-- CUSTOMIZE: Your frontend directory structure. Example:

- `src/` — Application source
  - `components/` — Shared UI components
  - `features/` — Feature modules with public entrypoints
  - `hooks/` — Shared React hooks
  - `services/` — API clients, persistence, integrations
  - `utils/` — Small shared utilities
  - `types/` — Shared TypeScript types
- `public/` — Static assets
-->

## Backend / API

<!-- CUSTOMIZE: Your backend directory structure. Example:

- `api/` or `server/` — Backend source
  - `routes/` or `controllers/` — HTTP route handlers
  - `services/` — Business logic
  - `models/` or `repositories/` — Data access
  - `middleware/` — Request processing pipeline
  - `utils/` — Server utilities
-->

## Config & Build

<!-- CUSTOMIZE: Your build and config files. Example:

- `package.json` / `pyproject.toml` / `go.mod` / `Cargo.toml` — Manifest
- `tsconfig.json` / `setup.cfg` — Language config
- `.eslintrc` / `ruff.toml` — Linter config
- `Dockerfile` — Container build
- `docker-compose.yml` — Local dev services
-->

## Tests

<!-- CUSTOMIZE: Your test directory structure. Example:

- `tests/` or `__tests__/` — Test files
  - `unit/` — Unit tests
  - `integration/` — Integration tests
  - `e2e/` — End-to-end tests
  - `fixtures/` — Test data and factories
-->
